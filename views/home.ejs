<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lista de dni</title>
</head>
<body>
  <h1>Lista de dni</h1>
  <ul>
    <% registros.forEach(registro => { %>
      <li>
        <%= registro.nombre %> - <%= registro.dni %> - <%= registro.identificador %>
        <form action="/eliminar/<%= registro.id %>" method="post" style="display:inline;">
          <button type="submit">Eliminar</button>
        </form>
        <form action="/modificar/<%= registro.id %>" method="post" style="display:inline;">
          <input type="text" name="nombre" value="<%= registro.nombre %>" placeholder="Nombre">
          <input type="text" name="dni" value="<%= registro.dni %>" placeholder="DNI">
          <input type="text" name="identificador" value="<%= registro.identificador %>" placeholder="Identificador">
          <button type="submit">Modificar</button>
        </form>
      </li>
    <% }); %>
  </ul>
  <h2>Agregar nuevo registro</h2>
  <form action="/agregar" method="post">
    <input type="text" name="nombre" placeholder="Nombre">
    <input type="text" name="dni" placeholder="DNI">
    <input type="text" name="identificador" placeholder="Identificador">
    <button type="submit">Agregar</button>
  </form>
</body>
</html>
